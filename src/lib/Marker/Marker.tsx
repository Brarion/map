import React from 'react';

type Props = {
  coords: [number, number],
  src: string,
  id: string,
  onClick: () => void,
  name?: string
}

const Marker: React.FC<Props> = () => null

export default Marker;